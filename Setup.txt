TestBuddy — Setup & Run Commands
================================

Prerequisites
-------------
- Node.js 20+
- PostgreSQL 18 (local)
  Database : testbuddy
  Username : admin
  Password : admin
  Host     : localhost
  Port     : 5432


1) Create PostgreSQL database (one-time)
---------------------------------------
Open psql as postgres, then run:

  CREATE ROLE admin LOGIN PASSWORD 'admin';
  CREATE DATABASE testbuddy OWNER admin;


2) Backend (Node.js / Express) — port 8080
------------------------------------------
cd backend
npm install
npm run dev

Health check: http://localhost:8080/api/health


3) Frontend (Vite React) — port 5173
------------------------------------
cd frontend
npm install
npm run dev

Open: http://localhost:5173


4) Extension (optional)
-----------------------
cd extension
npm install
npm run pack

Then either:
  - Download zip from http://localhost:5173/  (TestBuddy-extension.zip)
  - Or load unpacked folder: extension\dist

Chrome: chrome://extensions → Developer mode → Load unpacked
Select the unzipped "TestBuddy" folder (contains manifest.json).


5) AI service (title/description polish) — port 8001
----------------------------------------------------
cd ai-service
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt

# Create .env (copy from .env.example) and set ONE key:
#   GROQ_API_KEY=...          (recommended, free: https://console.groq.com/keys)
#   or OPENAI_API_KEY=...
#   or ANTHROPIC_API_KEY=...

uvicorn main:app --reload --port 8001

Health: http://localhost:8001/health
Polish: POST /ai/bug/polish  (called by backend only)

Also add to backend/.env:
  AI_SERVICE_URL=http://127.0.0.1:8001


Demo login
----------
Email    : alice@testbuddy.local
Password : password

Or register a new account at: http://localhost:5173/register


Useful URLs
-----------
Home / extension download : http://localhost:5173/
Login                     : http://localhost:5173/login
Register                  : http://localhost:5173/register
Bugs dashboard            : http://localhost:5173/bugs
Projects                  : http://localhost:5173/projects
Profile                   : http://localhost:5173/profile
Backend API               : http://localhost:8080/api
Extension zip (API)       : http://localhost:8080/api/extension/download
Bug export JSON           : GET /api/bugs/export/json
Bug import JSON           : POST /api/bugs/import


Notes
-----
- Start Backend before Frontend / Extension (they call localhost:8080).
- Copy backend/.env.example to backend/.env if needed (defaults match local Postgres).


Recent updates (30 Jul 2026)
----------------------------

Backend — Node.js / Express
  - Java/Spring Boot backend removed; use backend/ (Express + PostgreSQL + JWT).
  - Default DB: postgresql://admin:admin@localhost:5432/testbuddy
  - Seeded users password: password (see README for full list).

Extension — v0.3.0 recording improvements
  - On-page toolbar: Record / Pause / Stop / Screenshot (top frame only — no duplicate bar in iframes).
  - Screenshot flow: capture tab → draw red highlight → saves as a step.
  - Bug overview text can generate a step from your description.
  - Pack: cd extension && npm run pack
    → TestBuddy-extension.zip in frontend/public and backend/public/downloads

Frontend — light UI
  - Dashboard uses a light theme (white cards, slate text, teal accent).
  - Pages: / (home), /login, /register, /bugs, /projects, /profile
  - After UI changes: cd frontend && npm run build (or npm run dev for local work)

Regression checks (optional, from repo root)
  node scripts/regression.mjs
  node scripts/smoke-recording-api.mjs
  (Requires backend running on port 8080.)
